{% extends 'base.html.twig' %}

{% block title %}TypeBiere index{% endblock %}

{% block body %}
    <h1>TypeBiere index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>idTypeBiere</th>
                <th>NomTypeBiere</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_biere in type_bieres %}
            <tr>
                <td>{{ type_biere.idTypeBiere }}</td>
                <td>{{ type_biere.NomTypeBiere }}</td>
                <td>
                    <a href="{{ path('type_biere_show', {'idTypeBiere': type_biere.idTypeBiere}) }}">show</a>
                    <a href="{{ path('type_biere_edit', {'idTypeBiere': type_biere.idTypeBiere}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_biere_new') }}">Create new</a>
{% endblock %}