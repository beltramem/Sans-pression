{% extends 'base.html.twig' %}

{% block title %}Brasserie{% endblock %}

{% block body %}
    <h1>Brasserie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_brasserie</th>
                <td>{{ brasserie.id_brasserie }}</td>
            </tr>
            <tr>
                <th>Nom_brasserie</th>
                <td>{{ brasserie.nom_brasserie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('brasserie_index') }}">back to list</a>

    <a href="{{ path('brasserie_edit', {'id_brasserie': brasserie.id_brasserie}) }}">edit</a>

    {{ include('brasserie/_delete_form.html.twig') }}
{% endblock %}