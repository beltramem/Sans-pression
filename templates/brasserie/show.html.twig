{% extends 'base.html.twig' %}

{% block title %}Brasserie{% endblock %}

{% block body %}
    <h1>Brasserie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>idBrasserie</th>
                <td>{{ brasserie.idBrasserie }}</td>
            </tr>
            <tr>
                <th>nomBrasserie</th>
                <td>{{ brasserie.nomBrasserie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('brasserie_index') }}">back to list</a>

    <a href="{{ path('brasserie_edit', {'idBrasserie': brasserie.idBrasserie}) }}">edit</a>

    {{ include('brasserie/_delete_form.html.twig') }}
{% endblock %}