<form method="post" action="{{ path('brasserie_delete', {'id_brasserie': brasserie.id_brasserie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brasserie.id_brasserie) }}">
    <button class="btn">Delete</button>
</form>