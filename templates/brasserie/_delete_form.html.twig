<form method="post" action="{{ path('brasserie_delete', {'idBrasserie': brasserie.idBrasserie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brasserie.idBrasserie) }}">
    <button class="btn">Delete</button>
</form>