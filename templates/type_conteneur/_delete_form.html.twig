<form method="post" action="{{ path('type_conteneur_delete', {'id_type_conteneur': type_conteneur.id_type_conteneur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_conteneur.id_type_conteneur) }}">
    <button class="btn">Delete</button>
</form>