{% block body %}
	{% for type_conteneur in type_conteneurs %}
		<div class="col-md-12">
			<div class="col-md-12 glyphicon glyphicon-unchecked" id="{{ type_conteneur.idTypeConteneur }}-" onclick="CheckFiltre(this)">{{ type_conteneur }}</div>
		</div>
	{% else %}
		<p> pas de volume disponible </p>
	{% endfor %}
{% endblock %}