<form method="post" action="{{ path('vignoble_delete', {'IdVignoble': vignoble.IdVignoble}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vignoble.IdVignoble) }}">
    <button class="btn">Delete</button>
</form>